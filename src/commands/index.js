const Save = require('./save');
const Find = require('./find');
const List = require('./list');
const Remove = require('./remove');

module.exports = {
  save: Save,
  find: Find,
  list: List,
  remove: Remove,
}